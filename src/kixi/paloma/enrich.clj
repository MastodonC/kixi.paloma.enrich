(ns kixi.paloma.enrich
  (:require [kixi.paloma.enrich.file :as file])
  (:gen-class))




(comment
  1. Load the data from the LLPG
  2. Create a map of urpn as key against the row.
  3. For ones without a key.... a new group to enrich.


  )


(defn -main
  "Main function entry point."
  [& args]
  )
